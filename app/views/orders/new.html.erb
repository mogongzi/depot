<h1>New Order</h1>
<%= javascript_tag "$(\"input[value='Checkout'\]\").attr(\"disabled\", \"disabled\")" %>
<div class="depot_form">
  <fieldset>
    <legend>Please Enter Your Details</legend>
    <%= render 'form', order: @order %>
  </fieldset>  
</div>

<%= link_to 'Back', orders_path %>
